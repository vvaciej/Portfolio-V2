import { useTranslation } from 'next-i18next';

export const SpotifyBlock = () => {
	const { t } = useTranslation();
	return (
		<div className='w-full h-full bg-[#2C2C2E] col-span-2 sm:row-span-1 row-span-2 rounded-[2rem] flex xs:p-7 p-5'>
			
		</div>
	);
};
